import React from 'react';

import classes from './Sign_in_title.module.css';

const SignInTitle: React.FC = () => {
	return (
		<div className={classes.title}>Sign In to ArtSpace</div>
	);
};

export default SignInTitle;
