import React from 'react';

import classes from './Posts_header.module.css';

const Header: React.FC = () => {

	return (
			<div className={classes.title}>My posts</div>
	);
};

export default Header;
